lyrix:

clean:
    rm lyrix

install:
    mkdir -p $(DESTDIR)/usr/bin
    install -m 0755 lyrix $(DESTDIR)/usr/bin/lyrix
    chmod +x $(DESTDIR)/usr/bin/lyrix

uninstall:
    rm -f $(DESTDIR)/usr/bin/lyrix

